import Phaser from 'phaser';

class CodingPath extends Phaser.Scene {
  constructor() {
    super({
      key: 'CodingPath',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default CodingPath;
