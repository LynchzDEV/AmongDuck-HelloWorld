import Phaser from 'phaser';

class PathSelection extends Phaser.Scene {
  constructor() {
    super({
      key: 'PathSelection',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default PathSelection;
