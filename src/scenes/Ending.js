import Phaser from 'phaser';

class Ending extends Phaser.Scene {
  constructor() {
    super({
      key: 'Ending',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Ending;
