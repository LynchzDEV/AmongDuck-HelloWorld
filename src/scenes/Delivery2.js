import Phaser from "phaser";

class Delivery2 extends Phaser.Scene {
  constructor() {
    super({
      key: "Delivery2",
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Delivery2;