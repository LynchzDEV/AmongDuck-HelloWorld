import Phaser from 'phaser';

class FrontEnd extends Phaser.Scene {
  constructor() {
    super({
      key: 'FrontEnd',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default FrontEnd;
