import Phaser from 'phaser';

class CutScene1 extends Phaser.Scene {
  constructor() {
    super({
      key: 'CutScene1',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default CutScene1;
