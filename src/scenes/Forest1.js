import Phaser from 'phaser';

class Forest1 extends Phaser.Scene {
  constructor() {
    super({
      key: 'Forest1',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Forest1;
