import Phaser from "phaser";

class Delivery extends Phaser.Scene {
  constructor() {
    super({
      key: "Delivery",
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Delivery;