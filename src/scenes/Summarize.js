import Phaser from 'phaser';

class Summarize extends Phaser.Scene {
  constructor() {
    super({
      key: 'Summarize',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Summarize;
