import Phaser from 'phaser';

class DesignPath extends Phaser.Scene {
  constructor() {
    super({
      key: 'DesignPath',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default DesignPath;
