import Phaser from 'phaser';

class WebDesign extends Phaser.Scene {
  constructor() {
    super({
      key: 'WebDesign',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default WebDesign;
