import Phaser from 'phaser';

class Dev extends Phaser.Scene {
  constructor() {
    super({
      key: 'Dev',
    });
  }

  preload() {}

  create() {}

  update(delta, time) {}
}

export default Dev;
